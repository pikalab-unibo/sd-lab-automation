plugins {
  // Plugins affect the nature of the project
  // They add default tasks, and configure source sets
}

repositories {
  // Declare repositories for dependencies retrieval
}

application { // !Only available if the `application' plugin is declared!
    // Customise runnable applications here
    // For Java applications this is where you declare the main class
}

dependencies {
    // Declare dependencies here
}

// Task customisation here

// !Plugins may enable other sections!
