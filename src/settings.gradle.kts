rootProject.name = "root-project-name-here"

// !Include statements are for multi-project settings only!

// declares the sub-directory ./sub-project-1/ as a sub-project
include("sub-project-1")

// declares the sub-directory ./sub-project-2/ as a sub-project
include("sub-project-2")

// include other sub-projects here
